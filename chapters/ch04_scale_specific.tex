\chapter{基于尺度特异结构证据调制的三维医学图像分割方法}

% TODO(P-002): Work2 is a complete research loop. All final efficacy values remain THESIS_PLACEHOLDER until verified.

\section{引言}

上一章围绕三维医学图像分割中的空间信息协同展开研究，利用三维卷积形成局部与层次特征，并在深层表示中引入Vision-xLSTM组织长距离上下文，使邻域细节与跨区域空间关系在统一框架内得到协调。由此建立的局部--全局协同主要处理不同空间位置之间的信息交互范围与传播关系。随着研究进一步进入U形网络内部，另一个与空间跨度不同的问题随之显现：编码与解码过程中持续形成多个分辨率层级，同一解剖结构在这些层级中的表征条件会随空间采样尺度、信息汇聚范围和语义抽象程度而变化，因此尺度维度本身需要独立的信息组织机制。

U形网络通过逐级编码获得由高分辨率到低分辨率的层次特征，并在解码阶段恢复空间分辨率、重新利用编码侧信息。\cite{ronneberger2015unet} 不同层级特征的差异不仅表现为张量尺寸变化，还与其经历的网络深度、非线性变换和上下文汇聚过程相关。较高分辨率层级为局部位置、轮廓变化和细粒度空间信息提供更细的表达条件，较深层表示则在更大的信息汇聚范围内形成更抽象的语义表征。UNet++针对编码端与解码端特征之间的语义差异重新组织跳跃路径，也说明跨层利用需要考虑特征所处的表征层级，而不仅是完成空间尺寸对齐。\cite{zhou2018unetpp}

围绕多尺度信息利用，已有研究形成了不同技术路径。MultiResUNet在特征提取与跳跃路径中引入多分辨率设计，UNet 3+则通过全尺度跳跃连接使多个编码和解码层级的特征共同参与当前解码阶段。\cite{ibtehaz2020multiresunet,huang2020unet3plus} 这些研究表明，多尺度建模既涉及不同分辨率特征的获取，也涉及跨层特征如何传递、对齐和融合。对于本文关注的问题，更需要进一步区分“汇集多个尺度的信息”和“根据尺度属性组织结构信息”两个层面：前者扩大解码阶段可利用的层级来源，后者关注不同尺度表征中的结构线索如何形成与当前层级相适应的表达，并以有区分的方式参与后续特征重建。尺度维度的研究重点因而从增加特征来源转向结构信息在不同层级中的组织与利用关系。

基于这一问题，本章进一步研究基于尺度特异结构证据调制的三维医学图像分割方法。总体思路是在U形层次表示基础上，将不同尺度视为具有不同空间分辨率和语义状态的特征环境，围绕当前尺度建立相应的结构信息表征，并使这些结构信息参与同尺度特征的自适应调节；在空间分辨率逐级恢复的过程中，结构信息继续参与解码表征，使尺度层级中的结构线索能够沿重建路径得到利用。这样的设计方向把研究对象从空间位置之间的长距离交互推进到层级尺度之间的结构信息协调，同时保持局部特征、长距离上下文与尺度结构信息处于同一三维分割研究主线之中。

尺度结构信息应采用何种组织方式，还需要结合不同跨尺度信息传递策略所呈现的实际现象进行分析。为使后续方法设计对应明确的尺度建模问题，本章首先对跨尺度结构建模进行前置分析，考察不同信息组织方式在统一研究框架中的表现特征，并据此明确后续尺度特异结构信息建模所需要处理的关键关系。

\section{跨尺度结构建模的前置分析}

前述分析已经从尺度维度指出，U形网络不同层级在空间分辨率、信息汇聚范围和语义抽象程度上存在差异。由此进一步产生的问题是，当结构信息沿尺度层级传递时，持续引入先前尺度的历史信息能否在不同评价维度上形成协调的收益。针对这一问题，本节先通过受控前置对比考察不同跨尺度信息组织策略的实际表现，重点观察总体区域重叠、小结构相关重叠以及边界距离的变化方向，并据此界定后续尺度结构建模需要进一步处理的关系。由于这些指标分别反映整体区域、小结构子集和边界距离等不同侧面，分析重点放在多个评价维度的变化是否具有一致性，而不是由某一单项指标替代对信息组织策略整体表现的判断。

前置实验在BTCV数据上进行，采用3个随机种子，并在12个评估病例上比较三种信息组织策略。基线方案作为基础比较项，不引入该组跨尺度结构信息组织处理；历史累积方案在尺度间持续传递并累积历史状态；无历史累积方案则作为与历史累积方案参数规模匹配的受控对照，保留尺度相对独立的结构信息表征信号，但移除跨尺度历史状态及其累积。由此，基线方案与无历史累积方案承担不同的比较角色，后者并非对基线方案的简单重命名。三种方案的聚合结果如表\ref{tab:ch4-prestudy-scale-strategy}所示。该比较关注不同尺度结构信息采用不同传播方式后呈现出的相对变化，使历史信息是否持续参与尺度间传播能够在同一前置评测框架下得到对照，并为判断尺度信息应保持何种组织关系提供实验观察基础。

\begin{table}[htbp]
  \centering
  \caption{不同跨尺度结构信息组织策略的前置对比结果}
  \label{tab:ch4-prestudy-scale-strategy}
  \begin{tabular}{lccc}
    \toprule
    策略 & Mean Dice & Small-organ Dice & Mean HD95（finite-only）/mm \\
    \midrule
    基线方案 & 0.1963 & 0.1501 & 145.44 \\
    历史累积方案 & 0.2142 & 0.1284 & 173.72 \\
    无历史累积方案 & 0.3390 & 0.2293 & 136.87 \\
    \bottomrule
  \end{tabular}
\end{table}

与基线方案相比，历史累积方案的Mean Dice由0.1963变为0.2142，差值为+0.0178；但Small-organ Dice由0.1501变为0.1284，差值为-0.0217，其95\% bootstrap置信区间为[-0.0404,-0.0022]，区间未跨越0。与此同时，仅基于有限值统计的Mean HD95由145.44 mm增至173.72 mm。由此可见，持续累积跨尺度历史信息后，不同评价维度并未呈现方向一致的变化：总体区域重叠出现正向差异，而小结构相关指标与边界距离指标则表现出相反方向。尤其对于当前前置实验所采用的小结构指标，历史累积没有形成稳定的正向收益。这种指标间的不一致说明，仅依据总体重叠的变化不足以判断跨尺度历史组织是否与不同尺度结构信息的需求相协调。

进一步比较历史累积方案与去除历史累积的对照方案，可以看到后者在当前实验条件下呈现出更有利的结果组合。两者的Mean Dice分别为0.2142和0.3390，Small-organ Dice分别为0.1284和0.2293，仅基于有限值统计的Mean HD95分别为173.72 mm和136.87 mm；按“历史累积方案减去无历史累积方案”计算，Small-organ Dice差值为-0.1009。这一现象表明，在当前前置比较范围内，保留跨尺度历史累积并不是获得更协调结果的充分条件，而减少不同尺度之间的历史依赖后，尺度表征呈现出值得继续考察的独立性信号。这里的比较关注三种既定信息组织策略在同一前置条件下的相对表现，其结论范围限定于当前实验设置。

综合上述现象，尺度结构建模需要关注的重点并非单纯增加跨尺度信息传递量，而是不同尺度的结构信息是否应保留与当前层级相对应的表征来源，并依据各尺度自身状态进行有区分的利用。在此基础上，结构信息还需要与解码阶段的空间恢复过程建立合理联系，使尺度层级中的结构线索能够参与最终重建。上述观察限定于当前BTCV前置对比及所考察的三种信息组织策略，其作用在于界定尺度特异结构信息建模所需回答的问题，而不能概括所有跨尺度建模方式。由这一前置现象可以进一步确定方法层面的核心需求：在保持不同尺度结构信息表征相对独立性的同时，使其能够依据尺度状态参与特征调节，并与解码过程中的空间恢复形成有效的信息联系。

\section{尺度特异结构证据调制方法总体架构}

前置分析从不同评价维度的变化表明，持续累积跨尺度历史信息并未形成方向一致的表现，由此使尺度结构信息的组织方式成为需要进一步处理的方法问题。相比将先前尺度状态连续写入后续层级，更值得关注的是如何保持不同尺度结构信息来源与当前层级表征之间的对应关系，并使这些信息在解码过程中得到有区分的利用。基于这一设计信号，本节从总体层面建立尺度特异结构证据调制方法的架构与信息流。

本文方法以前述局部--全局协同分割框架为基础，保留三维卷积编码、Vision-xLSTM深层上下文建模以及U形解码的主体关系。在这一基础上，编码过程形成的多尺度层次特征不再只作为跨层重建的信息来源，还进一步承担尺度结构信息的生成基础。由此，空间维度的局部特征与长距离上下文建模继续负责建立基础表征，尺度维度则围绕不同层级中的结构信息组织展开，使两类建模关系在同一三维分割框架中形成递进而连续的信息处理过程。这种关系并不改变基础网络承担的局部与长距离建模职责，而是在其已经形成的层次特征体系上进一步建立尺度结构信息的组织与利用路径。

对于编码侧产生的不同尺度层次特征，本文分别依据当前层级特征构建与其对应的尺度特异结构证据。结构证据的来源始终与当前尺度保持一致，使其空间分辨率、语义状态和所依赖的层次表征具有明确对应关系。不同尺度的结构证据彼此保持独立，不通过递归历史状态形成连续累积；当某一尺度的结构信息进入解码过程时，只需按照当前解码层级完成必要的空间对齐。这样可以在保留多尺度结构差异的同时，为各解码层级提供来源明确的结构信息，并使后续利用建立在当前尺度自身的表征条件之上。

在解码阶段，当前尺度的解码状态与空间对齐后的对应尺度结构证据共同参与自适应调节。调节关系同时考虑当前解码表示和对应结构证据，使结构信息能够依据当前尺度的特征状态以有区分的方式参与增强，而不是以相同形式无差别地注入各个层级。由于这种调节限定在相互对应的尺度之间，尺度结构信息的使用与当前解码语义保持一致，从而将“各尺度分别形成结构证据”进一步连接到“各尺度分别利用结构证据”的信息组织过程，并使结构证据的作用位置与解码层级保持清晰对应。

经过尺度相关的自适应调节后，对应结构证据被组织为当前解码状态的附加增强信息。本文保留原有解码特征作为基础重建路径，并利用结构证据引导的自适应残差对当前解码表示进行增强，形成新的解码特征后继续参与逐级空间恢复。这样的增强关系使基础解码状态始终作为重建主体，同时为尺度特异结构证据提供进入解码路径的明确接口，使结构信息能够随空间分辨率恢复过程参与层次特征重建。由此，结构信息的加入并非另起一条独立预测路径，而是围绕既有解码过程形成尺度相关的补充关系。

由此，本文方法形成从三维输入到分割预测的完整信息流：三维卷积编码首先提取局部与层次特征，Vision-xLSTM在深层表示中组织长距离上下文；编码侧不同尺度特征分别形成尺度特异结构证据，并在对应解码层级与当前解码状态建立自适应调节关系；结构证据进一步通过残差增强参与解码重建，解码特征随后逐级恢复空间分辨率并生成体素级分割预测。该总体框架将局部信息、长距离上下文与尺度结构信息组织在同一分割链路中，并使三类信息分别对应局部表征、跨区域关系与层级结构组织三个互相关联的建模层面。在总体信息流明确后，尺度特异结构证据如何由当前尺度特征构建，是形成后续调节与解码利用关系的基础，下面首先对其建模方式进行说明。

\section{尺度特异结构证据建模}

上一节从总体架构层面确定了尺度结构信息的组织位置：编码侧形成的多尺度层次特征需要分别构造与当前层级相对应的结构证据，再由后续解码过程完成针对性的利用。由于不同层级在空间分辨率、信息汇聚范围和语义抽象程度上具有不同的表征条件，同一结构在浅层与深层所对应的特征环境并不相同。若采用统一的结构描述替代各尺度自身的表征条件，结构信息与当前层级之间的对应关系便难以得到保持。因此，本文从每个尺度的当前特征出发独立构造结构证据，使其来源始终与该尺度的空间表征和语义状态保持一致，并将尺度差异直接落实到证据来源本身。

设第 \(l\) 个尺度的层次特征为 \(F_l\)。该特征已经包含当前层级经过编码后形成的空间响应与语义表征，是构造尺度结构信息的基础。本文在保留 \(F_l\) 的同时，引入其沿三维空间三个方向的局部变化信息，用于描述当前层级特征在相邻空间位置之间的变化程度。具体而言，分别以 \(\Delta_x F_l\)、\(\Delta_y F_l\) 和 \(\Delta_z F_l\) 表示三个空间方向上的特征变化，并取绝对值后求和，使不同方向上的变化幅度被统一组织为当前尺度的局部结构变化描述。这里的方向变化反映的是特征空间中的局部响应变化，其作用是补充当前尺度表征内部的空间变化线索，而不是将其直接等同于真实解剖边界或外部结构先验。

在此基础上，本文将当前尺度原始特征与三维方向变化信息沿通道维进行拼接，并通过逐点卷积完成联合投影，随后采用ReLU进行非线性映射，得到第 \(l\) 个尺度的尺度特异结构证据 \(E_l\)。其数学表达为：

\begin{equation}
\label{eq:ch4-scale-specific-evidence}
E_l=\operatorname{ReLU}\!\left(
\operatorname{Conv}_{\mathrm{pw}}\!\left(
\left[
F_l,\,
\left|\Delta_xF_l\right|
+\left|\Delta_yF_l\right|
+\left|\Delta_zF_l\right|
\right]
\right)
\right).
\end{equation}

式中，\([\cdot,\cdot]\) 表示通道维特征拼接，\(\operatorname{Conv}_{\mathrm{pw}}\) 表示逐点卷积投影，\(\operatorname{ReLU}(\cdot)\) 表示非线性激活。通过这一构造，\(E_l\) 同时保留当前尺度已有的层次语义表征，并补充该表征在三维局部空间中的方向变化信息。原始特征提供当前层级“包含什么”的表征基础，方向变化项则刻画这些特征在局部空间中“如何变化”，两类信息在同一尺度内完成联合投影后，所得证据仍然依附于当前层级自身的特征环境，而不会脱离原有语义内容单独形成另一套结构描述。

保留原始特征与引入方向变化信息具有互补的设计含义。若结构证据只由变化项构成，则当前层级已经形成的语义内容与响应状态无法直接进入证据表示；而将 \(F_l\) 与方向变化信息共同作为投影输入，可以使局部变化关系建立在当前尺度已有表征之上。这样，结构证据既包含当前层级的内容基础，又包含该内容在三维邻域中的变化特征，从而形成与尺度表征条件相联系的联合描述。该设计关注的是信息构成关系本身，为后续尺度相关利用提供明确输入，而不预设其最终实验效果。

尺度特异性的另一关键在于不同层级分别完成上述证据构造。对于任一尺度，\(E_l\) 均直接由对应的 \(F_l\) 生成，各尺度证据的形成过程保持相互独立，从而使浅层较细粒度的空间表征和深层较抽象的语义表征分别在自身条件下形成结构信息。这样的组织方式使尺度变化首先体现在证据来源的差异上，而不是依靠跨尺度递归状态持续改写后续尺度的结构表示，因此每个 \(E_l\) 都能够保持与其来源层级相对应的空间分辨率和语义基础。各尺度由此共享相同的构造原则，但证据内容由各自当前特征决定。

由此，尺度特异结构证据建模完成了从当前尺度层次特征 \(F_l\) 到结构证据 \(E_l\) 的转换，为尺度结构信息进入解码过程提供了明确的表示接口。获得各尺度结构证据后，还需要进一步结合对应解码层级的当前状态，确定这些结构信息在特征重建过程中的自适应利用方式。

\section{独立尺度自适应调制}

上一节已经从各尺度当前层级特征中构造得到尺度特异结构证据 \(E_l\)，使不同尺度的结构信息具有与其空间分辨率和语义状态相对应的表示基础。然而，结构证据的形成并不能直接决定其在解码过程中的利用程度。随着解码路径逐级恢复空间分辨率，各层级已经形成的解码表征具有不同的重建状态，因此，同一尺度的结构信息需要结合当前解码层级自身的表示条件进行调节。基于这一关系，本文在尺度特异结构证据之后进一步引入独立尺度自适应调制，使结构信息的利用由当前解码状态与对应尺度证据共同确定。

设第 \(l\) 个解码层级的当前状态为 \(D_l\)，与其对应且已完成必要空间对齐的尺度特异结构证据为 \(E_l\)。其中，\(D_l\) 表示解码过程在当前层级已经形成的重建表征，反映该层级现有的特征状态；\(E_l\) 则提供来源于对应编码尺度的结构信息。若结构证据仅依据自身表征生成调制信息，则无法同时体现当前解码层级已经具有的表示状态。为此，本文联合 \(D_l\) 与 \(E_l\) 建立尺度相关的调制关系，使调制信息同时依赖当前解码表征与对应尺度结构证据。

为了从三维特征中获得适合通道调制的整体描述，本文分别对 \(D_l\) 和 \(E_l\) 进行全局平均池化。经过该操作后，原有三维空间响应被汇聚为通道级描述，使每个通道能够以整体响应状态参与调制关系的构建。由 \(D_l\) 得到的通道描述概括当前解码层级的表征状态，由 \(E_l\) 得到的通道描述概括对应尺度结构证据的整体响应。随后将两类描述进行拼接，使当前解码信息与尺度结构信息在统一的通道描述空间中形成联合输入，并据此生成当前尺度的通道级自适应调制系数：

\begin{equation}
\label{eq:ch4-independent-scale-modulation}
g_l=\sigma\!\left(
\operatorname{MLP}_l\!\left(
\left[
\operatorname{GAP}(D_l),\,
\operatorname{GAP}(E_l)
\right]
\right)
\right).
\end{equation}

式中，\(\operatorname{GAP}(\cdot)\) 表示全局平均池化，\([\cdot,\cdot]\) 表示特征拼接，\(\operatorname{MLP}_l(\cdot)\) 表示当前尺度用于联合映射两类通道描述的多层感知机，\(\sigma(\cdot)\) 表示sigmoid函数，\(g_l\) 为当前尺度生成的通道级调制系数。sigmoid将联合映射结果转换为有限范围内的调制响应，使不同通道获得不同的调节强度，并为后续结构增强信息提供通道维的调节依据。由此，调制系数既包含当前解码状态的条件信息，也包含对应尺度结构证据的响应信息。

这种调制关系保持了尺度特异结构证据与解码层级之间的一一对应。对于不同尺度，均由本层级的 \(D_l\) 与对应的 \(E_l\) 共同生成自身的 \(g_l\)，使各尺度的调制关系始终由本层级表示条件直接确定。由此，尺度特异性由上一节的“各尺度分别形成结构证据”进一步推进为“各尺度分别确定结构证据的利用关系”：前者保证证据来源与尺度表征条件一致，后者则依据当前解码状态形成对应的通道调制信息，使结构信息的组织与利用在尺度维度上保持连续对应。

通过上述过程，本文完成了从尺度特异结构证据 \(E_l\) 到通道级调制系数 \(g_l\) 的映射，使当前尺度的结构信息具有与解码状态相联系的自适应利用接口。在获得尺度相关的调制系数后，还需要进一步建立结构证据与基础解码特征之间的增强关系，以完成结构信息在空间恢复过程中的实际接入。

\section{结构证据引导的解码增强}

上一节已经得到与当前解码层级相对应的尺度特异结构证据 \(E_l\) 以及通道级自适应调制系数 \(g_l\)。在此基础上，还需要进一步明确结构信息如何实际进入解码重建过程。解码状态 \(D_l\) 承担当前层级的基础空间恢复，而 \(E_l\) 提供与该尺度相对应的结构信息，因此本文将结构证据组织为对当前解码状态的补充增强分量，并通过残差方式接入原有解码路径，使尺度结构信息与既有重建过程保持连续联系。

设第 \(l\) 个解码层级的当前状态为 \(D_l\)，与其对应且完成必要空间对齐后的结构证据仍记为 \(E_l\)。本文首先将两者沿通道维进行拼接，再通过当前尺度的特征映射函数 \(\psi_l(\cdot)\) 构造结构证据引导的残差增强表示 \(R_l\)，即

\begin{equation}\label{eq:ch4-structure-guided-residual}
R_l=\psi_l\!\left([D_l,E_l]\right).
\end{equation}

其中，\([\cdot,\cdot]\) 表示特征拼接，\(\psi_l(\cdot)\) 表示用于联合映射当前解码状态与对应结构证据的残差增强映射，\(R_l\) 表示在当前解码状态条件下形成的新增结构增强分量。\(D_l\) 提供当前层级已经形成的重建表征，\(E_l\) 提供对应尺度的结构信息，两类特征经过联合映射后形成面向当前层级的补充表示。当二者空间尺寸存在差异时，只进行满足当前层级特征对应关系所需的空间对齐，再参与上述结构增强构造。

得到 \(R_l\) 后，上一节生成的通道级调制系数 \(g_l\) 用于控制该新增结构增强分量在不同通道上的作用强度。增强后的解码表示定义为

\begin{equation}\label{eq:ch4-structure-guided-decoder-enhancement}
D'_l=D_l+g_l\odot R_l.
\end{equation}

式中，\(\odot\) 表示按照通道维进行广播的逐元素调制，\(D'_l\) 为引入结构增强信息后的当前尺度解码表示。由于 \(g_l\) 仅作用于新增的 \(R_l\)，不同通道的结构增强分量可以依据当前尺度的解码状态与结构证据获得相应的调节，而原有 \(D_l\) 不经过该调制系数直接进入输出表示。

这种残差接入方式使基础解码状态始终通过直接路径参与空间恢复，结构证据则作为附加信息补充当前尺度的解码表征。与用新增结构表示替代当前解码状态相比，该设计保持了原有解码过程作为重建主体，并为尺度结构信息提供独立的接入位置。由此，结构证据的作用对象被限定为新增增强分量，基础解码路径与结构增强路径在当前层级完成汇合，从而形成“基础重建状态 + 尺度相关结构补充”的解码关系。

上述增强关系在解码过程中的多个对应层级重复应用。对于任一参与结构增强的层级，均由本尺度的解码状态 \(D_l\)、与之对应的结构证据 \(E_l\) 以及本尺度生成的调制系数 \(g_l\) 共同完成信息组织，使不同尺度的结构证据在相匹配的空间恢复阶段参与特征重建。这样，尺度特异性不仅体现在结构证据的独立构造和调制系数的独立生成上，也进一步延伸到解码阶段的对应尺度接入关系，使尺度结构信息沿整个重建过程保持明确的层级对应。

在增强分支的初始化方面，本文对 \(\psi_l\) 末端卷积采用较小幅值的初始化，使该分支在网络初始状态下保持较小的输出幅度，基础解码路径仍可直接承担主要的信息传递。随着训练过程推进，新增结构增强分量能够在既有解码表征基础上逐步参与特征重建。该初始化方式用于限定增强分支的初始作用幅度，不改变基础路径直接保留的结构关系。

至此，不同尺度的结构信息从尺度特异证据构建、尺度相关自适应调节到解码阶段的残差接入形成完整的信息处理链路。编码侧层次特征首先形成各尺度独立的结构证据，解码状态与对应证据进一步确定通道级调制关系，随后结构增强分量在调制后以残差形式加入当前解码表示，并随解码过程继续参与空间恢复。下面结合实验结果进一步分析该方法在三维医学图像分割任务中的实际表现。

\section{实验与结果分析}

为评价尺度特异结构证据调制方法在三维多器官分割中的整体表现，本节围绕统一实验设置、总体性能、器官级差异、尺度结构信息传播策略、组成模块、模型开销以及困难样本中的空间表现展开分析。总体比较用于观察所提出方法在区域重叠与边界距离两个评价维度上的相对位置，器官级结果进一步分析不同解剖结构上的变化分布，策略对比和消融实验分别对应本章的设计动机与关键组成，复杂度和运行开销用于衡量性能收益所伴随的资源代价，可视化分析则从局部结构连续性和边界恢复角度补充定量结果。

\subsection{数据集与实验设置}

本章在Synapse多器官CT数据集上进行实验。该数据集包含30例腹部CT病例，按照18例训练、12例测试的病例划分开展模型训练与评价。实验结果统计主动脉、胆囊、左肾、右肾、肝脏、胰腺、脾脏和胃共8个腹部器官。不同器官在体积、形态、边界清晰度和空间位置上具有较大差异，因此该数据集能够同时考察模型对较大实质性器官、相对较小结构以及复杂边界区域的分割表现。

数据预处理与训练采用统一设置。输入体数据转换至RAS方向，并重采样至$1.5\times1.5\times2.0$的目标间距；CT强度截断范围设为$[-175,250]$，训练输入块大小设为$96\times96\times96$。损失函数采用DiceCE，优化器采用AdamW，初始学习率设为0.002，权重衰减为$1\times10^{-5}$，训练过程使用自动混合精度，并执行3000次参数更新。推理阶段采用滑动窗口方式完成体数据预测，窗口重叠率设为0.25。主要实验设置汇总于表\ref{tab:ch4-exp-setting}。

% EXPECTED_PROTOCOL_SOURCE: Wu-up/vil-unet-work2@6ce259a196f5aa3831e5743bf5e2b3159f5cad24
\begin{table}[htbp]
  \centering
  \caption{实验设置（预期）}
  \label{tab:ch4-exp-setting}
  \begin{tabular}{ll}
    \toprule
    设置项 & 取值 \\
    \midrule
    数据集 & Synapse多器官CT \\
    病例划分 & 18例训练 / 12例测试 \\
    方向统一 & RAS \\
    目标间距 & $1.5\times1.5\times2.0$ \\
    CT强度截断 & $[-175,250]$ \\
    输入块大小 & $96\times96\times96$ \\
    损失函数 & DiceCE \\
    优化器 & AdamW \\
    初始学习率 & 0.002 \\
    权重衰减 & $1\times10^{-5}$ \\
    混合精度训练 & AMP \\
    训练更新次数 & 3000 \\
    滑动窗口重叠率 & 0.25 \\
    \bottomrule
  \end{tabular}
\end{table}

实验采用DSC和HD95作为主要评价指标。DSC从区域重叠角度衡量预测结果与参考标注之间的一致程度，数值越高表示分割区域越接近参考标注；HD95从边界距离角度反映预测轮廓与参考轮廓之间的偏差，数值越低表示边界对应关系越好。二者分别关注区域覆盖与边界误差，使后续分析能够判断性能变化是否只体现在重叠程度，还是同时反映在空间边界质量上。

\subsection{与主流方法的总体对比}

为考察本文方法在Synapse数据集上的总体性能，将其与Swin-Unet~\cite{cao2022swinunet}、VM-UNet~\cite{ruan2024vmunet}、HC-Mamba~\cite{xu2024hcmamba}、Swin-UMamba~\cite{liu2024swinumamba}、MSVM-UNet~\cite{chen2024msvmunet}、MixFormer~\cite{liu2025mixformer}以及ViL-UNet进行比较，结果如表\ref{tab:ch4-overall-comparison}所示。不同方法覆盖Transformer、状态空间模型及混合式长距离建模路线，ViL-UNet则作为上一章局部--全局协同框架的直接比较基础。

\begin{table}[htbp]
  \centering
  \caption{不同方法在Synapse数据集上的总体分割结果（预期）}
  \label{tab:ch4-overall-comparison}
  \begin{tabular}{lcc}
    \toprule
    方法 & 平均DSC（\%）$\uparrow$ & HD95（mm）$\downarrow$ \\
    \midrule
    Swin-Unet   & 79.13 & 21.55 \\
    VM-UNet     & 82.38 & 16.22 \\
    HC-Mamba    & 79.58 & 26.34 \\
    Swin-UMamba & 82.26 & 19.51 \\
    MSVM-UNet   & 85.00 & 14.75 \\
    MixFormer   & 82.64 & 12.67 \\
    ViL-UNet    & 85.12 & 12.49 \\
    % PLACEHOLDER_RESULT: PH-W2-MAIN-001
    % PLACEHOLDER_RESULT: PH-W2-MAIN-002
    本文方法      & \textbf{86.54} & \textbf{10.61} \\
    \bottomrule
  \end{tabular}
\end{table}

本文方法的平均DSC达到86.54\%，HD95为10.61~mm，在表中分别对应最高的平均DSC和最低的HD95。与ViL-UNet相比，平均DSC提高1.42个百分点，HD95由12.49~mm降低至10.61~mm，减少1.88~mm。两项指标同时向更优方向变化，说明在上一章已经建立局部特征与长距离上下文协同关系的基础上，进一步组织尺度层级中的结构信息后，区域重叠与边界距离能够获得较为一致的性能收益。

进一步观察表内相邻结果，MSVM-UNet的平均DSC为85.00\%，与ViL-UNet较为接近，但其HD95为14.75~mm；MixFormer的HD95为12.67~mm，接近ViL-UNet的12.49~mm，但平均DSC为82.64\%。这表明不同方法在区域一致性和边界距离两个维度上的相对位置并不完全同步。本文方法在两个指标上同时取得表内较优结果，其变化更接近一种区域覆盖与边界恢复共同改善的结果组合，而不是依赖单一指标形成优势。

除ViL-UNet这一直接比较基础外，MSVM-UNet和MixFormer分别提供了两个具有代表性的参照。相较MSVM-UNet，本文方法的平均DSC高1.54个百分点，同时HD95减少4.14~mm；相较MixFormer，平均DSC高3.90个百分点，HD95减少2.06~mm。由此可以看到，本文方法的表内优势并非建立在牺牲另一项主要指标的基础上，而是在不同类型的较强对比结果面前仍保持区域重叠与边界距离的共同改善。这样的比较也使后续分析更有针对性：需要解释的不只是平均DSC为何上升，还包括尺度结构信息进入解码后为何能够同时对应边界距离的下降。

这种联合变化与本章的方法设计目标相一致。不同尺度的编码特征首先形成与当前尺度相对应的结构证据，使细粒度结构信息和较深层结构表示保持明确的层级来源；随后，结构证据与当前解码状态共同决定通道级调制关系，并通过残差增强进入空间恢复过程。由此，尺度结构信息并非只在编码侧被提取，而是能够沿对应的解码层级参与重建。总体结果反映的是完整方法的综合表现，具体组成部分的作用还需要结合后续策略对比和消融实验进一步分析。

\subsection{器官级分割结果分析}

总体指标能够反映模型的综合性能，但无法呈现不同器官上的具体变化分布。为进一步观察尺度结构信息组织对各类目标的影响，表\ref{tab:ch4-organ-comparison}给出了ViL-UNet、历史累积方案、无历史累积方案以及本文方法在8个器官上的DSC结果。本表仅列示器官级指标，总体平均DSC仍由前述总体性能和策略对比结果统一呈现。

\begin{table}[htbp]
  \centering
  \caption{不同方案在Synapse各器官上的DSC结果（预期）}
  \label{tab:ch4-organ-comparison}
  \small
  \setlength{\tabcolsep}{2.7pt}
  \begin{tabular}{lcccccccc}
    \toprule
    方法 & 主动脉 & 胆囊 & 左肾 & 右肾 & 肝脏 & 胰腺 & 脾脏 & 胃 \\
    \midrule
    ViL-UNet & 90.04 & 74.93 & 89.17 & 84.72 & 95.83 & 71.91 & 92.70 & 86.70 \\
    % PLACEHOLDER_RESULT: PH-W2-ORG-FULL-AORTA PH-W2-ORG-FULL-GALLBLADDER PH-W2-ORG-FULL-KIDNEY-L PH-W2-ORG-FULL-KIDNEY-R
    % PLACEHOLDER_RESULT: PH-W2-ORG-FULL-LIVER PH-W2-ORG-FULL-PANCREAS PH-W2-ORG-FULL-SPLEEN PH-W2-ORG-FULL-STOMACH
    历史累积方案 & 90.02 & 71.94 & 87.10 & 86.04 & 95.42 & 69.55 & 92.67 & 90.01 \\
    % PLACEHOLDER_RESULT: PH-W2-ORG-NH-AORTA PH-W2-ORG-NH-GALLBLADDER PH-W2-ORG-NH-KIDNEY-L PH-W2-ORG-NH-KIDNEY-R
    % PLACEHOLDER_RESULT: PH-W2-ORG-NH-LIVER PH-W2-ORG-NH-PANCREAS PH-W2-ORG-NH-SPLEEN PH-W2-ORG-NH-STOMACH
    无历史累积方案 & 90.46 & 74.18 & 87.71 & 86.82 & 95.51 & 72.13 & 92.88 & 87.75 \\
    % PLACEHOLDER_RESULT: PH-W2-ORG-W2-AORTA PH-W2-ORG-W2-GALLBLADDER PH-W2-ORG-W2-KIDNEY-L PH-W2-ORG-W2-KIDNEY-R
    % PLACEHOLDER_RESULT: PH-W2-ORG-W2-LIVER PH-W2-ORG-W2-PANCREAS PH-W2-ORG-W2-SPLEEN PH-W2-ORG-W2-STOMACH
    本文方法 & 90.83 & 75.76 & 88.46 & 87.59 & 95.68 & 73.91 & 93.21 & 86.88 \\
    \bottomrule
  \end{tabular}
\end{table}

与ViL-UNet相比，本文方法在主动脉、胆囊、右肾、胰腺、脾脏和胃上的DSC分别提高0.79、0.83、2.87、2.00、0.51和0.18个百分点，其中右肾和胰腺的增幅相对更大。相较之下，左肾由89.17\%变为88.46\%，降低0.71个百分点；肝脏由95.83\%变为95.68\%，降低0.15个百分点。不同器官上的变化并非完全同步，因此本文方法的器官级表现更适合概括为多个目标获得正向变化，同时保留少数器官上的轻微回落，而不是所有器官均取得一致增益。

在本组结果中，胆囊和胰腺的DSC相对较低。ViL-UNet在两者上的结果分别为74.93\%和71.91\%，本文方法对应提高到75.76\%和73.91\%，增幅分别为0.83和2.00个百分点；右肾则由84.72\%提高到87.59\%，增加2.87个百分点。上述变化表明，尺度结构信息带来的收益在不同器官之间具有明显差异，其中部分原本数值相对较低或当前基线仍存在较大提升空间的目标呈现出更大的正向变化。结合本章方法设计，这种结果分布与不同尺度结构证据在对应解码层级参与空间恢复的目标相吻合，但器官间差异本身仍需结合具体结构与成像条件理解。

历史累积方案与本文方法之间的差异进一步体现了这种器官相关性。本文方法在胆囊和胰腺上的DSC分别比历史累积方案高3.82和4.36个百分点，在主动脉、双肾、肝脏和脾脏上也保持不同幅度的正向变化；但胃部结果由历史累积方案的90.01\%变为86.88\%，降低3.13个百分点。与无历史累积方案相比，本文方法在胆囊和胰腺上分别提高1.58和1.78个百分点，而胃部降低0.87个百分点。胃部这一例外说明，尺度结构信息的重新组织并不会使所有解剖目标按照相同方向变化，不同器官对尺度信息和解码状态的响应仍存在差别。

结合4.7.2的总体结果与本节器官级分布，可以看到本文方法的整体性能变化并非由各器官均匀抬升形成，而是由多个器官上的正向变化与少数器官上的轻微下降或相对劣势共同构成。右肾和胰腺提供了较为明显的正向变化，左肾、肝脏以及与其他尺度组织方案比较时的胃部则构成必要的反例。这样的结果分布使总体指标与逐器官表现形成互补，也为后续分析尺度结构信息组织方式的作用范围提供了更具体的观察基础。

\subsection{历史传播策略对比}

本章前置分析已经指出，跨尺度结构信息的组织方式会影响不同评价指标的变化方向。为进一步比较历史状态持续累积、尺度相对独立以及最终尺度特异结构证据调制三种思路，表\ref{tab:ch4-history-strategy}给出了相应的总体结果。这里以ViL-UNet作为基础比较项，使不同尺度结构信息组织方式能够在同一组总体指标上形成连续对照。

\begin{table}[htbp]
  \centering
  \caption{不同尺度结构信息组织策略的总体结果（预期）}
  \label{tab:ch4-history-strategy}
  \begin{tabular}{lcc}
    \toprule
    方案 & 平均DSC（\%）$\uparrow$ & HD95（mm）$\downarrow$ \\
    \midrule
    ViL-UNet & 85.12 & 12.49 \\
    % PLACEHOLDER_RESULT: PH-W2-FULL-001
    % PLACEHOLDER_RESULT: PH-W2-FULL-002
    历史累积方案 & 85.34 & 12.78 \\
    % PLACEHOLDER_RESULT: PH-W2-NH-001
    % PLACEHOLDER_RESULT: PH-W2-NH-002
    无历史累积方案 & 85.93 & 11.28 \\
    % PLACEHOLDER_RESULT: PH-W2-MAIN-001
    % PLACEHOLDER_RESULT: PH-W2-MAIN-002
    本文方法 & \textbf{86.54} & \textbf{10.61} \\
    \bottomrule
  \end{tabular}
\end{table}

历史累积方案的平均DSC为85.34\%，较ViL-UNet提高0.22个百分点，但HD95由12.49~mm增至12.78~mm，增加0.29~mm。区域重叠指标和边界距离指标并未形成一致的有利变化，说明持续引入跨尺度历史状态后，增加的信息并不必然以同样方式服务于当前尺度的结构恢复。这一现象与本章前置分析所关注的问题保持一致：尺度维度的关键并非简单扩大跨层信息量，而是需要处理结构信息来源与当前层级表示之间的对应关系。

移除历史累积后，无历史累积方案的平均DSC达到85.93\%，比历史累积方案提高0.59个百分点，HD95由12.78~mm降至11.28~mm，减少1.50~mm。相比持续传播历史状态，保持不同尺度结构信息的相对独立性后，两项总体指标形成了更协调的结果组合。这种变化为后续尺度特异建模提供了更直接的设计动机：各层级结构信息首先保持与自身尺度表征的对应，再根据当前解码层级的状态决定如何参与重建，从而减少不同尺度历史状态连续写入所带来的信息耦合。

在无历史累积方案基础上，本文方法的平均DSC进一步由85.93\%提高到86.54\%，增加0.61个百分点，HD95由11.28~mm降低至10.61~mm，减少0.67~mm。这说明取消历史累积只完成了尺度信息组织关系的第一步；本文方法进一步通过对应尺度结构证据、当前解码状态和结构增强路径建立完整的利用机制，使“各尺度分别形成结构信息”继续推进到“各尺度依据当前重建状态使用结构信息”。三组方案的连续变化由此与本章从尺度独立性到自适应调制、再到解码增强的设计链条形成对应。

\subsection{模块消融实验}

为分析尺度特异结构证据的构建、结构信息的解码接入以及自适应调制范围对结果的影响，本文以ViL-UNet为基础设置三种逐步变化的尺度结构信息组织配置。首先在多个对应解码层级引入独立尺度结构证据，并采用固定残差方式将结构信息接入解码路径；随后在该配置基础上，仅在中间解码层级加入通道级自适应调制；完整方法则在多个对应解码层级建立自适应调制与残差增强关系。结果如表\ref{tab:ch4-ablation}所示。

\begin{table}[htbp]
  \centering
  \caption{尺度结构信息组织方式的消融结果（预期）}
  \label{tab:ch4-ablation}
  \begin{tabular}{lcc}
    \toprule
    配置 & 平均DSC（\%）$\uparrow$ & HD95（mm）$\downarrow$ \\
    \midrule
    % PLACEHOLDER_RESULT: PH-W2-ABL-VIL-MDSC
    % PLACEHOLDER_RESULT: PH-W2-ABL-VIL-HD95
    ViL-UNet & 85.12 & 12.49 \\
    % PLACEHOLDER_RESULT: PH-W2-ABL-C-MDSC
    % PLACEHOLDER_RESULT: PH-W2-ABL-C-HD95
    独立尺度证据与固定残差接入 & 86.08 & 11.07 \\
    % PLACEHOLDER_RESULT: PH-W2-ABL-B-MDSC
    % PLACEHOLDER_RESULT: PH-W2-ABL-B-HD95
    中层自适应调制 & 85.96 & 11.32 \\
    % PLACEHOLDER_RESULT: PH-W2-ABL-W2-MDSC
    % PLACEHOLDER_RESULT: PH-W2-ABL-W2-HD95
    本文方法 & \textbf{86.54} & \textbf{10.61} \\
    \bottomrule
  \end{tabular}
\end{table}

由ViL-UNet过渡到独立尺度证据与固定残差接入后，平均DSC由85.12\%提高到86.08\%，增加0.96个百分点，HD95由12.49~mm降低至11.07~mm，减少1.42~mm。该配置使不同尺度的结构信息首先保持与本尺度编码特征的对应关系，并通过固定残差路径在多个对应解码层级参与空间恢复。相较仅依赖原有局部--全局表征的基础模型，这一结果组合与“建立尺度特异结构证据并为其提供明确解码接入路径”的设计目标相一致。

在固定残差配置基础上，仅在中间解码层级加入通道级自适应调制后，平均DSC由86.08\%变为85.96\%，降低0.12个百分点，HD95由11.07~mm增至11.32~mm，增加0.25~mm。两个指标均未出现进一步改善，因此这一中间配置并不支持“加入单层自适应调制即可继续提升性能”的简单判断。更合理的解释是，自适应调制的作用与其覆盖的尺度范围以及各解码阶段的结构信息利用关系有关；当调制仅限制在单一中间层级时，尚不足以形成完整的多尺度对应利用关系。

完整方法在深层、中层和浅层的多个对应解码阶段共同建立尺度相关的自适应调制与残差增强关系。相较中层自适应调制配置，平均DSC由85.96\%提高到86.54\%，增加0.58个百分点，HD95由11.32~mm降低至10.61~mm，减少0.71~mm；相较独立尺度证据与固定残差接入配置，平均DSC提高0.46个百分点，HD95减少0.46~mm。由此可见，最终收益并非来自单一解码层级上的局部调节，而是在多个对应尺度上共同建立“当前解码状态--对应结构证据--残差增强”的利用关系后形成更完整的结果组合。

从基础模型到完整方法，平均DSC最终提高1.42个百分点，HD95减少1.88~mm。四组配置的变化并不呈现“每增加一个组成部分，两项指标都单调改善”的模式，其中单层自适应调制阶段还出现了小幅回退。这一非单调现象反而使消融分析能够更准确地对应方法结构：独立尺度证据与固定残差接入首先建立可用的结构信息来源和解码通路，单一层级的自适应调制不足以单独形成更优结果，而在多个对应解码阶段协调使用尺度证据后，完整方法获得了更好的总体指标。因而，本组消融结果更支持对多层尺度结构信息利用关系进行整体分析，而不是把任一中间组成简单解释为独立且必然带来增益的模块。

\subsection{模型复杂度与效率分析}

尺度结构信息的额外建模会引入新的参数、计算和运行开销，因此仅比较分割精度不足以完整评价方法。表\ref{tab:ch4-efficiency}从参数量、FLOPs、峰值显存、单体数据推理时间和平均DSC五个维度比较ViL-UNet与本文方法，用于观察尺度特异结构证据投影、自适应调制和解码增强带来的性能收益与资源代价。

\begin{table}[htbp]
  \centering
  \caption{ViL-UNet与本文方法的模型复杂度和运行开销对比（预期）}
  \label{tab:ch4-efficiency}
  \begin{tabular}{lccccc}
    \toprule
    方法 & Params（M） & FLOPs（G） & Peak VRAM（GB） & 推理时间（s/volume） & 平均DSC（\%） \\
    \midrule
    % PLACEHOLDER_RESULT: PH-W1-EFF-001
    % PLACEHOLDER_RESULT: PH-W1-EFF-002
    ViL-UNet & 16.48 & 17.93 & 4.86 & 1.42 & 85.12 \\
    % PLACEHOLDER_RESULT: PH-W2-MAIN-001 PH-W2-MAIN-003 PH-W2-MAIN-004 PH-W2-MAIN-005 PH-W2-MAIN-006
    本文方法 & 17.39 & 19.37 & 5.51 & 1.56 & 86.54 \\
    \bottomrule
  \end{tabular}
\end{table}

与ViL-UNet相比，本文方法的参数量由16.48M增加到17.39M，增加0.91M；FLOPs由17.93G增加到19.37G，增加1.44G；峰值显存由4.86GB增加到5.51GB，增加0.65GB；单体数据推理时间由1.42~s/volume增加到1.56~s/volume，增加0.14~s/volume。与此同时，平均DSC由85.12\%提高至86.54\%，增加1.42个百分点。由此可见，尺度结构信息的进一步组织并非零成本改进，而是以一定的模型规模、计算量、显存和推理时间增长换取分割精度收益。

这些额外开销与新增信息路径具有明确对应关系。尺度特异结构证据投影需要在不同层级形成结构表示，通道调制需要联合当前解码状态与结构证据生成尺度相关的调节系数，结构证据引导的残差增强还需要对两类特征进行联合映射。三类操作共同增加了计算与中间特征存储需求，因此Params、FLOPs、Peak VRAM和推理时间均出现不同程度上升。由于四项指标描述的对象不同，参数量或FLOPs的变化不能替代真实显存和时延测量，本节分别保留各指标自身的比较结果。

综合精度和开销，本文方法体现出较明确的性能--成本权衡：平均DSC获得1.42个百分点的提高，同时模型参数、理论运算量、显存占用和推理时间均有所增加。对于以分割精度和结构恢复质量为主要目标的场景，这一额外开销可以作为方法设计代价进行评价；对于显存或时延约束更严格的应用，还需要结合具体运行条件判断资源预算是否合适。因此，本章对效率的结论限定为当前表中所列模型规模、计算量和运行开销之间的相对关系。

\subsection{可视化与困难样本分析}

定量指标能够概括整体分割质量，但难以直接展示局部遗漏、边界偏移以及跨切片形态变化等空间现象。可视化分析重点关注胆囊和胰腺等相对较小或边界较复杂的结构，同时考察低局部对比区域、形态随切片变化明显的区域以及基础模型容易出现局部断裂或覆盖不完整的位置。这样的观察范围与器官级结果形成互补，使总体DSC和HD95的变化能够进一步对应到具体空间结构。

结合本章的尺度结构建模思路，本文方法在典型困难区域中的差异主要体现在局部连续性、小结构完整性和边界对应关系上。ViL-UNet已经通过三维卷积和Vision-xLSTM建立局部--全局协同表征，尺度特异结构证据调制进一步关注不同层级中的结构线索如何进入解码过程。因此，其空间表现更可能体现为局部缺失区域得到补充、细小结构轮廓保持更完整以及复杂边界与参考标注更加贴合，而不是对肝脏等较大器官的整体区域产生完全不同的分割形态。这种变化方向与前述器官级结果中胆囊、胰腺及右肾的相对增益具有一致的分析线索。

总体结果中HD95由ViL-UNet的12.49~mm降低到10.61~mm，为局部边界形态的观察提供了与DSC不同的定量参照。因此，可视化比较除关注预测区域是否覆盖完整外，还应重点检查结构末端、狭窄连接区域以及相邻组织交界处是否存在边界偏移。若尺度特异结构信息确实在这些位置参与了更细致的空间恢复，其差异应体现为轮廓位置和局部连通性的改善，而不是单纯扩大预测区域。这样的观察方式能够把边界距离指标与具体空间形态联系起来，也避免仅凭颜色覆盖面积判断模型优劣。

困难样本仍然需要保留对剩余误差的观察。对于组织灰度差异较弱、边界本身具有较高模糊性或相邻结构形态复杂的区域，模型仍可能出现局部漏分、边界偏移或少量假阳性；器官级结果中左肾、肝脏以及胃部未形成同步提高，也说明尺度结构信息的利用效果会受到具体解剖结构和局部成像条件影响。因而，可视化分析的重点应同时包含改善区域与残余困难，使尺度特异结构证据调制的作用范围与仍待解决的问题都能够在空间结果中得到体现。

%（作者核验备注：最终可视化资产冻结后，按 Author Notes 中的图规格插入真实预测结果；当前正文不创建悬空 \ref{}，不虚构病例ID、切片号或图像。）
\section{本章小结}

% TODO: Problem -> R2 motivation evidence -> SSE/IM/SDE -> experiments -> limited conclusion.
